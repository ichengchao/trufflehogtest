name: TruffleHog Secrets Scan
on: [push]
jobs:
  TruffleHog:
    runs-on: ubuntu-latest
    container:
      image: trufflesecurity/trufflehog:latest
    steps:
      - uses: actions/checkout@v3
      - name: Checkout code test 1111
        id: mystep
        continue-on-error: true
        run: |
          rm -rf /__w/trufflehogtest/trufflehogtest/.git
          trufflehog filesystem /__w/trufflehogtest/trufflehogtest --no-verification --no-update --fail
      - name: send msg
        if: ${{steps.mystep.outcome == 'failure11'}}
        run: |
          echo "error   876868767878608760876"
      - name: send msg 34
        run: |
          echo "error   333333"